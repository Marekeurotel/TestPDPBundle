# TestPDPBundle

Test Playwright (pytest) sprawdzający obecność oferty zestawu na PDP.

## Uruchomienie lokalne

```bash
python3 -m pip install -r requirements.txt
pytest -q tests/test_pdp_bundle_offer.py
```

